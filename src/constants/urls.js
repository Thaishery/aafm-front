// const port = "8000";
const port = "8081";
const url = {
  /* Localhost */
  // apiUrl: `http://localhost:${port}`,

  // pprod
  // apiUrl: `https://pprod.aafm.gdeb.fr:${port}`
  
  // prod
  apiUrl: `https://aafm-grigny.fr:${port}`
  
};

export default url